import PaceConverter from '../../containers/PaceConverter/PaceConverter';
import { BackDrop } from '../../styles/layout';

const Page = () => {
	return (
		<BackDrop>
			<PaceConverter />
		</BackDrop>
	);
};

export default Page;
